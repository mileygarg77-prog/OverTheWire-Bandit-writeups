# Bandit Level 17 → 18

## Goal
Two files, `passwords.old` and `passwords.new`, differ by exactly one line — the new password.

## Steps
```
diff passwords.old passwords.new
```

## Lesson
`diff` is the direct tool for spotting changes between near-identical files instead of manual comparison.
