## Bandit Level 7 → 8

## Goal
Password is in `data.txt`, on the same line as the word `millionth`.

## Steps
```
grep millionth data.txt
```

## Lesson
`grep` for a known nearby keyword instead of manually reading huge files.
